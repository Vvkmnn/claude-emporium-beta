{
  "hooks": {
    "PostToolUseFailure": [{
      "matcher": "Bash|Edit",
      "hooks": [{ "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/post-error.js" }],
      "description": "Observe tool failure patterns (silent on success)"
    }],
    "Stop": [{
      "matcher": "*",
      "hooks": [{ "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/stop.js" }],
      "description": "Reflect on unprocessed observations before session ends"
    }]
  }
}
